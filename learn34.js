// JavaScript Function with Parameter
// create functions with parameters
// a and b are parameters
var count = (a, b) => {
    resultCount = a * b;
    alert("Hasil dari a * b = " + resultCount);
}

// then call the parameter like this
count(2, 3); //  2 is a, and 3 is b