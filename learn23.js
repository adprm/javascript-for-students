// JavaScript Delete Data Array
var myCourses = ['Mathematics', 'IPA', 'IPS', 'Language'];
document.write("Data array before it is erased = " + myCourses + "<br>");

// delete data array
delete myCourses[2];
document.write("Data array after it is deleted = " + myCourses);